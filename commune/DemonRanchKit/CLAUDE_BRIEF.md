CLAUDE_BRIEF — Demon Ranch research session (Andy's machine)
=============================================================
You are joining Financial Demonology: the study of volatility harvesting
(Shannon's Demon / Fernholz rebalancing premium) as a full portfolio lens.
Public doctrine: thisisez.com/demon-codex.html ; data: /demon-atlas.html.
This kit is the portable lab of Alex Adamczyk's Demon Ranch campaign
(research layer codename Fable). House epistemics, non-negotiable:
  - Ledger over theory; append-only results (JSONL vaults, never edited)
  - Parity gates: no engine change ships without agreement vs the canonical
    stdlib engine (src/demon_ranch/backtest_duos.py simulate())
  - 12-universe tournament = the judge (survival across physics, not
    leaderboards); hodl gate is a column, not a filter
  - Selection-bias honesty; every quoted return carries its window
  - NO live trading, NO accounts, NO advice. Research only.

CURRENT STATE (2026-07-13): engine v3.1 (chunked execution + 10bps quantum)
gated and blessed; full 168,588-cell multiverse rerun in progress on Alex's
machine (atlas_v31.jsonl). The vault format: one JSON line per (config,
policy, universe) cell with per-world finals/holds/drawdowns/rebalances.

GOOD FRIDAY TASKS (pick by available time; all runnable from kit root):
  1. SHARD WORK: run the --shard slice Alex assigns; ship the .jsonl back.
  2. THE COIN LABORATORY (sidecars/coin-kelly-20260712/): extend the
     primitives program — two-coin correlation dial (verify gamma exactly),
     k-coin 1/k^2 dilution check, drift-regime coin (insurance in the toy).
     Cheap, exact, docket item 34.
  3. SHADOW TWELVE design (docket item 9): draft U13-U24 friction/tax
     universe generators as make_world extensions (DO NOT overwrite U1-U12).
  4. Query play: load any vault JSONL and build leaderboards (see
     sidecars/demon-atlas-20260709/query_early.py for the card pattern).
RULES: new experiments = new dated sidecar dirs; never edit existing vaults;
gate any engine change; write findings to a REPORT.md in your sidecar.
Send back: new sidecars + shard JSONLs. Alex merges and judges.

GIT DISCIPLINE (the commune repo: degenai/diamondlegendz.com)
  - NEVER commit to main. Work on a branch (commune/<name>-<topic>), PR to main.
  - New work = new dated sidecar dir under commune/DemonRanchKit/sidecars/.
    Never edit existing sidecars or anyone else's files.
  - Vault JSONLs NEVER enter git (they are 100MB-1GB). Shard results travel
    gzipped via Drive/USB; only small derived JSONs (<5MB) may be committed.
  - Findings go in a REPORT.md inside your sidecar; Alex merges and judges.
