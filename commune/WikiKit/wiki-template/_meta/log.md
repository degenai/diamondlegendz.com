---
visibility: internal
---

# Log

Append-only. Diagnostic, not narrative — record *what changed and when*, terse. Retrieval is by grep ("when did X enter?"), not by browsing. The story layer is the captain's log (`captains-log/`); this stays mechanical.

Format: `ACTION PageName — note`. Actions in use: CREATED, UPDATED, RENAMED, ARCHIVED, CAPTAINS-LOG.

## 2026-07-16 (WikiKit install)
INSTALLED WikiKit — personal wiki patched in from alexpedia (captain's-log edition). First commit; the graph starts here.
CREATED meta — ported the design doc from alexpedia: the no-index divergence plus the captain's-log narrative layer.
CREATED Alex, Andy, diamondlegendz, DemonRanchKit, financial-demonology, accordion-tab-forge, the-compute-commune — sanitized seed nodes from alexpedia.
CREATED Marceline, Olivia — family stubs, names reserved, content Andy's to write.
